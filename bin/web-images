#!/usr/bin/env bash

# Uses ImageMagick.
# Resize to 1200px width and remove metadata (-strip flag).
# Todo: Add check for the convert binary.

if [[ ! $1 ]]; then
	echo "Usage: web-images <targetWidth>";
	exit 1;
fi

for image in *; do
	convert $image -resize $1 -strip $image;
done;

exit 0;
